/*
class RecipeRepository = {
  constructor(recipeData) {
    this.currentUser: ????
    this.recipes = listRecipes(recipeData); [Recipe, Recipe, Recipe, Recipe]
  }
  listRecipies(data) {
    iterate the data argument
    create Recipe objects here using Recipe class: new Recipe(data.id, data.img..etc)
    return Recipe objects to an array
    
  }
  filterRecipeTag([tag, tag, tag]) {
   - filter array of recipies using an array of tags 
   - This will return a new list of recipies based on the tag(s)
  }

  filterRecipeName("recipeName") {
    - filter array of recipies using a recipe name
    - Return an array of recipies based on user input

  }

  filterRecipeIngredient("ingredient") {
    - filter array of recipies using an ingredient passed in 
    - Return an array of recipies based on user input

  }
}

*/
